import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
  wrapper: {
    position: 'relative',
    minHeight: '100%',
  },
  contentWrapper: {
    display: 'flex',
    justifyContent: 'center',
    height: '100%',
  },
});
